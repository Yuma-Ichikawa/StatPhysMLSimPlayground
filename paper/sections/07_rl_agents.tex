\section{Cross-domain validation}
\label{sec:crossdomain}

The following domains are canonical validation cases, not coequal mechanism
studies and not evidence for shared equilibrium exponents.

\subsection{Diffusion trajectories}

The control is reverse time and the secondary coordinate controls locality.
We track speciation, nonlocality, trajectory commitment, and proximity to
memorized samples. The oracle anchor separates speciation from later collapse
\cite{biroli2024dynamical}; the holdout asks whether their ordering survives a
deformed score trajectory.

\begin{figure}[t]
\centering
\includegraphics[width=0.96\linewidth]{figures/figure4_diffusion.pdf}
\caption{Diffusion finite-size validation. Raw order, response,
pseudocritical drift, and largest-size predictive checks are shown separately.}
\label{fig:diffusion}
\end{figure}

\subsection{Reinforcement learning}

RL is organized by KL pressure $\beta_{\rm KL}$ and verifier noise
$\epsilon_{\rm ver}$. Gold reward, strategy support, and Goodhart gap are
joint observables; entropy alone cannot distinguish productive specialization
from collapse \cite{gao2023reward}. The two-dimensional map tests whether an
oracle-calibrated critical region predicts the holdout feedback dynamics.

\begin{figure}[t]
\centering
\includegraphics[width=0.96\linewidth]{figures/figure5_reinforcement.pdf}
\caption{RL validation in the $(\beta_{\rm KL},\epsilon_{\rm ver})$ plane.
Color encodes gold reward; contours and companion diagnostics show strategy
support and Goodhart behavior with uncertainty inherited from outer seeds.}
\label{fig:rl}
\end{figure}

\subsection{Multi-agent populations}

Intrinsic evidence $h$ and social coupling $J$ are separate controls. A global
flip prevents consensus from being mistaken for truth, and population size
$A$ remains explicit. The heatmap distinguishes field-driven alignment,
interaction-driven consensus, fragmentation, and error amplification.

\begin{figure}[t]
\centering
\includegraphics[width=0.96\linewidth]{figures/figure6_multiagent.pdf}
\caption{Multi-agent validation in the $(h,J)$ plane with finite-population
boundary overlays. The order is truth-conditioned consensus, not agreement
alone.}
\label{fig:agents}
\end{figure}

